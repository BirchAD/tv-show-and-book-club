<div>
  <h1>Your Clubs</h1>
  <div class="cards">
    <% @clubs.each do |club| %>
      <div class="card">
        <h4> <%=link_to club.name, club_path(club) %> </h4>
      </div>
    <% end %>
  </div>
</div>
